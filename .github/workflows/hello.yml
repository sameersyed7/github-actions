#Goal : say Hi and show the repo name
name: Hello Dosto
on:
  workflow_dispatch:
    

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echoe "Hello Dosto"
      - name: greet in english
        run: echoe "Welcome"

  repo_name:
    needs: greet
    runs-on: ubuntu-latest
    steps:
      - name: repo name
        run: echo " ${{ github.event.repository.name }}"
      
